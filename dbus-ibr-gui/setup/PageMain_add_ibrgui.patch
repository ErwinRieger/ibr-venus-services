--- /opt/victronenergy/gui/qml/PageMain.qml.orig	2025-10-28 08:06:26.747499325 +0100
+++ /opt/victronenergy/gui/qml/PageMain.qml	2025-12-01 16:12:42.555024539 +0100
@@ -154,6 +154,7 @@
 		var name = service.name
 
 		var page
+		console.log("processing service " + name)
 		switch(service.type)
 		{
 		case DBusService.DBUS_SERVICE_MULTI:
@@ -162,6 +163,10 @@
 		case DBusService.DBUS_SERVICE_MULTI_RS:
 			return;
 		case DBusService.DBUS_SERVICE_BATTERY:
+		    if (name == "com.victronenergy.battery.ibrbms") {
+                var comp = Qt.createComponent("PageIbrServices.qml");
+		        deviceList.append(service, comp.createObject(root, {service: service, bindPrefix: service.name}))
+            }
 			page = batteryPage
 			break;
 		case DBusService.DBUS_SERVICE_SOLAR_CHARGER:
